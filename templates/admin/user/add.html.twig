{% extends 'admin/base.html.twig' %}

{% block title %}Administrators{% endblock %}

{% form_theme form _self %}

{% block body %}
    <div class="container">
        <h3 class="admin-h3">Add User</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary btn-admin"><i class="fas fa-check"></i>Add</button>
        {{ form_end(form) }}
    </div>
{% endblock %}